# 8/26 — 상속

### 상속이란 무엇인가?
- **Inheritance**
- 부모 클래스의 멤버 함수, 멤버 변수 등을 자식 클래스에서도 활용할 수 있게 하는 것

---

### 상속은 왜 사용하는가?
- 한 클래스에서 만들었던 멤버들을 다른 클래스에서 재활용하기 위해서

---

### 부모 클래스와 자식 클래스의 관계는?
- 자식 클래스는 부모 클래스의 멤버 함수, 멤버 변수들을 사용할 수 있다.
- 반드시 자식과 부모가 **IS-A 관계**일 때만 상속을 사용하여야 한다.
- 부모가 자식에게 멤버 접근을 허용하려면 `public` 또는 `protected` 접근지정자를 사용한다.

---

### 상속받은 객체는 메모리에 어떻게 존재하는가?
- **부모 클래스의 메모리 + 자식 클래스에 추가된 멤버 메모리**
- 객체가 만들어질 때는 **부모 ➡️ 자식** 순으로 생성자 호출
- 소멸할 때는 **자식 ➡️ 부모** 순으로 소멸자 호출

---

### 상속과 코드 재사용의 관계는?
- 상속은 부모 클래스의 멤버들을 가져오기 때문에 필요한 함수가 있다면 자식 클래스가 부모 클래스에서 상속받은 멤버 함수를 호출할 수 있다.
- `Animal` 같은 부모 클래스가 있다면 `Dog`, `Cat` 같은 자식 클래스에서 `Animal`의 멤버를 활용하기 용이하다.

---

### 상속과 다형성은 어떤 관계가 있는가?
- 부모 클래스와 자식 클래스에 같은 이름의 멤버가 있을 수 있다.
- 다형성을 위해 부모 포인터로 자식 객체를 가리킬 수 있는데, 이때 부모 포인터로 멤버를 호출할 때 `virtual`이 없으면 부모 것, 부모에 `virtual`이 붙어있으면 자식 것이 호출된다.
- 부모 클래스의 소멸자를 가상 소멸자(`virtual`)로 선언하지 않으면 메모리 누수가 발생할 수 있다.
