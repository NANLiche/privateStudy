# 8/29 — 다형성

### 다형성이란 무엇인가?
모습은 같은데 형태가 다른것

### C++에서 다형성은 어떻게 구현되는가?
오버라이딩, 오버로딩, 혹은 템플릿을 통해 구현된다

### 컴파일 타임 다형성과 런타임 다형성은 무엇인가?
컴파일 타임 다형성은 컴파일 도중 이루어지는 다형성,
런타임 다형성은 런타임 도중 이루어지는 다형성을 의미한다.
(컴파일 타임 - 오버로딩, 템플릿 / 런타임 - virtual+오버라이딩)

### 부모 타입으로 자식 객체를 다루면 어떤 일이 일어나는가?
부모타입에서 virtual로 정의되지 않은 함수라면
같은 이름의 멤버를 호출했을때
부모타입의 멤버들이 호출이된다.

### 다형성이 필요한 상황은 무엇인가?
동일한 기능을 하는 함수가 다루는 자료형이 다를 때
부모 클래스에서 함수의 활용을 자식클래스에게 일임하고 싶을 때

---

### 게임 서버에서 다형성을 사용할 만한 상황은 무엇인가?
*(제미나이에게 빌려온 지식)*

**1. 게임 내 오브젝트(Entity) 관리**
MMORPG 같은 게임에는 플레이어, 몬스터, NPC, 드롭된 아이템 등 수많은 오브젝트가 존재합니다.
- **부모 클래스:** `GameObject` (가상 함수: `Update()`, `OnDamaged()`, `Die()`)
- **자식 클래스:** `Player`, `Monster`, `NPC`
- **다형성 활용:** 서버의 메인 루프(Game Loop)에서는 이들이 구체적으로 어떤 객체인지 알 필요가 없습니다. 그저 `std::vector<GameObject*> objects`를 순회하면서 `obj->Update()`만 호출하면, 플레이어는 플레이어의 로직을, 몬스터는 몬스터의 AI 로직을 각자 알아서 실행합니다.

**2. 네트워크 패킷(Packet) 처리**
서버는 클라이언트로부터 이동, 공격, 채팅, 아이템 사용 등 수백 가지의 다른 패킷을 받습니다.
- **부모 클래스:** `PacketHandler` (또는 `IPacket`) (가상 함수: `Execute()`)
- **자식 클래스:** `MovePacketHandler`, `AttackPacketHandler`, `ChatPacketHandler`
- **다형성 활용:** 네트워크 I/O 스레드는 패킷의 ID를 보고 알맞은 핸들러 객체를 생성한 뒤, `handler->Execute()`만 호출합니다. 새로운 기능(예: 길드 생성)이 추가되더라도 기존 네트워크 수신 코드는 수정할 필요 없이 `GuildCreatePacketHandler` 클래스만 새로 만들면 되므로 유지보수가 매우 편해집니다.

**3. 몬스터/보스의 상태 머신 (FSM - Finite State Machine)**
몬스터의 AI는 대기(Idle), 추적(Chase), 공격(Attack), 도망(Flee) 등의 상태를 가집니다.
- **부모 클래스:** `State` (가상 함수: `Enter()`, `Update()`, `Exit()`)
- **자식 클래스:** `StateIdle`, `StateChase`, `StateAttack`
- **다형성 활용:** 몬스터 객체는 현재 상태를 나타내는 `State* currentState;` 포인터만 가지고 있습니다. 매 프레임마다 `currentState->Update()`를 호출하면, 현재 몬스터가 어떤 상태인지에 따라 알아서 다르게 행동합니다.

**4. 데이터베이스(DB) 및 로깅(Logging) 시스템**
서버의 로그를 콘솔에 찍을지, 파일로 남길지, 외부 DB(ElasticSearch 등)로 보낼지 결정할 때 쓰입니다.
- **부모 클래스:** `ILogger` (가상 함수: `WriteLog()`)
- **자식 클래스:** `ConsoleLogger`, `FileLogger`, `DBLogger`
- **다형성 활용:** 서버 초기화 시 설정 파일에 따라 적절한 자식 객체를 생성하여 부모 포인터에 할당해두면, 게임 로직 코드에서는 `Logger->WriteLog()`만 호출해도 원하는 곳에 로그가 남게 됩니다.
