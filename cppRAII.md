# 8/24 — RAII

## RAII란 무엇인가?

**Resource Acquisition Is Initialization**

자원 획득은 초기화  
(객체와 자원의 라이프 사이클을 일치시킨다)

C++의 설계 기법 중 하나

메모리 누수등을 방지하는 설계 기법이다.

---

## RAII는 왜 필요한가?

프로그래머가 직접 자원을 획득하고 관리하는 것이 아니라

자원의 생성, 파괴, 관리를 모두 객체에 위임하여

메모리 누수등을 방지하고 안전을 보장하기 위해

---

## RAII와 객체의 수명은 어떤 관계가 있는가?

객체와 자원의 라이프 사이클을 일치 시키기 때문에

객체가 생성될 때 동적메모리를 할당하고,

객체가 소멸할 때 자동으로 자원을 해제시킨다.

즉, 객체의 수명 = 할당한 자원의 수명 이되도록

객체 자신이 직접 조절하는 것이다.

---

## 자원(Resource)이란 무엇인가?

사용 후에 시스템에 돌려주는 모든 것

---

## 동적할당된 메모리를 사용하는 RAII는 C++에서 어떻게 구현되는가?

보통 스마트 포인터를 통하여 구현된다.

원한다면 클래스의 생성/소멸자를 통해 직접구현도 가능하다.

---

## RAII와 생성자/소멸자의 관계는?

생성자에서 자원을 획득하고 소멸자에서 해제한다.

---

## RAII가 메모리 누수를 어떻게 방지하는가?

생성자에서 자원을 획득하고 소멸자에서 해제하여서.

---

## 서버 코드에서 RAII가 왜 중요한가?

서버가 오랫동안 실행되면서 수많은 자원을 사용하고,

여러 프로그래머들이 협업하며 코드가 길어지다보면

일일히 자원을 해제해주기 어려워지기 때문이다.

---

# 잼쌤의견

학습하신 내용에 더해, 알아두면 좋은 몇 가지 추가 개념을 덧붙여 드립니다.

## 1. 예외 안전성 (Exception Safety) 보장

RAII가 강력한 가장 큰 이유 중 하나입니다. 코드를 실행하다가 중간에 예외(Exception)가 발생해 함수가 비정상적으로 종료될 때, 수동으로 관리하는 자원은 `delete`나 `close`가 호출되지 않아 누수가 발생합니다.

하지만 RAII를 사용하면 함수를 빠져나갈 때 스택 풀림(Stack Unwinding) 현상이 발생하면서 지역 객체의 소멸자가 무조건 자동으로 호출되므로, 예외 상황에서도 100% 자원 해제를 보장할 수 있습니다.

## 2. 메모리 외의 다양한 '자원(Resource)'

정리하신 대로 '시스템에 돌려줘야 하는 모든 것'이 자원입니다. 메모리 외에도 다음과 같은 것들이 RAII를 통해 주로 관리됩니다.

- **스레드 동기화 객체 (Mutex)**
  - `std::lock_guard`
  - `std::unique_lock`
  - (가장 대표적인 RAII 객체)

- **파일 입출력 (File Stream)**
  - `std::ifstream`
  - `std::ofstream`
  - (소멸자에서 자동으로 파일을 닫아줌)

- **네트워크 소켓 (Network Socket) 및 DB 커넥션**
