# 9/3 — unique_ptr

### unique_ptr이란 무엇인가?
동적 할당된 메모리에 대해 독점적 소유권을 가지는 스마트 포인터

### unique_ptr은 왜 필요한가?
뭐 어찌저찌 예외가 발생되는 바람에 메모리 해제를 하지않아서 누수가 생기는 것을 방지하기 위해.

### unique_ptr의 소유권은 무엇을 의미하는가?
하나의 동적 메모리의 소유권을 얻으면은 그 메모리에 대한 책임을 진다. 여기서 책임이란 자기자신이 파괴될 때 자신이 소유한 객체도 함께 파괴할 책임.

### unique_ptr의 객체 수명은 어떻게 관리되는가?
유니크 포인터는 생성된 중괄호 스코프를 벗어나면 자동으로 소멸자가 호출된다.

### unique_ptr은 어떻게 생성하고 사용하는가?
```cpp
std::unique_ptr<int> p1 = std::make_unique<int>(10);
```
요런식으로 할당을해서 포인터처럼 쓸 수 있다.

```cpp
std::unique_ptr<AClass> p2 = std::make_unique<AClass>(); p2->Func();
```
처럼 클래스도 생성할 수 있다.

### unique_ptr의 소유권을 이동시키려면 어떻게 하는가?
유니크 포인터는 독점적 소유권을 가지기 때문에

```cpp
std::unique_ptr<int> p2 = std::move(p1);
```
move를 이용하여 소유권을 옮겨줘야한다.

(p1는 null 포인터가 된다잇)

### unique_ptr과 RAII는 어떤 관계인가?
유니크 포인터는 객체의 수명을 자동화하여 컨트롤해주기 때문에 전형적인 RAII를 구현한 클래스이다.

### unique_ptr을 복사할 수 없는 이유는?
하나의 메모리를 두 개의 포인터가 가르키게되면 독점적이지 않게되기때문에 복사할 수 없다.
두개로 가르키면 나중에 메모리가 이중으로 해제될 수도 있음.

### make_unique or new
make_unique는 객체를 생성하면서 unique_ptr이 바로 소유하도록 만들어주는 함수이고, 직접 new하는 것보다 안전하고 간결하다.

