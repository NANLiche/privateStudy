포인터에 대해 알고 있는 대로 적기
2026-08-11

포인터란? 포인팅 해주는 것. 가르켜는 것. 
무엇을 가르키냐하면, 주소를 가르키는 것이다.
이때 주소란, 포인터가 가르키는 변수의 메모리 주소를 의미한다.

예시는 아래와 같다.

int a = 80;
int *ptr = &a;

&는 주소 변환자이다. &a처럼 사용시, 변수 a의 주소를 반환한다.
ptr이 a의 주소를 가르켜고 있다.

cout<<ptr;

이런식으로 사용시 0xf123... 과 같이 16진수로 이루어진 a의 주소가 반환된다.

만약 a의 값을 출력하고 싶다면 어떻게 할까?

cout<<*ptr;
결과: 80

다음과 같이 ptr앞에 *를 붙여줌으로서 포인터가 가르키는 변수의 값을 얻어올수있다.

이때 *는 앞서 사용한 int *의 *과 달리 주소를 주척하는 연산자이다.


이번에는 배열과 포인터의 관계에 대해 알아보도록하자.

int a[2] = {1, 2};
int b = 3;
int * ptr = &b;

다음과 같은 문장들이 있다.

이때 a 배열의 시작점의 주소를 알고 싶다고 하자.

방법은 두가지다

cout<<a;
또는
cout<<&a[0];

어느쪽이든 a의 주솟값을 나타낸다.

만약 ptr = a 로 ptr에 배열 a의 주솟값을 담았다고 하자.

그렇다면 ptr[0], ptr[1]과 같은 식들도 유효하다고 볼수 있다.


const와 포인터

const int a = 1
int const a = 1

위 두 식은 본질 적으로 차이가 없다.
두 식 모두 a의 값을 수정하는 것이 불가능하다.

다만, 포인터가 개입할시 의미가 크게 달라진다.

const int * ptr
int const * ptr

둘다 const int, 또는 int const 자료형을 가진 변수의 주솟값을 담을 수 있는 포인터이다.

const * int ptr

int * const ptr
const * int * ptr
int * const * ptr

