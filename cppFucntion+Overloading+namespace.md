# 8/16 — 함수 / 오버로딩 / namespace + 총복습

### 함수란 무엇인가?
* 특정한 기능을 실행하는 코드의 묶음

### 함수의 매개변수와 반환값은 무엇인가?
* 함수의 매개변수는 함수가 매개로 받는 값이고,
* 반환값은 함수가 실행된뒤 반환하는 값이다.

### 값 전달과 주소/참조 전달은 어떻게 다른가?
* **값전달**은 `func(int a)`처럼 값을 전달하여 a를 사용하는데는 새로운 메모리가 필요하다(물론 함수가 끝날때는 회수된다.)  
  그리고 이러한 까닭에 원본 변수의 값은 변하지 않는다.
* **주소/참조 전달**은 `func(int *a)` 또는 `func(int &a)`처럼 주소나 참조로 전달되어,  
  이러한 전달의 경우 원본의 값을 함수내에서 변화시킬 수 있다.

### 함수 오버로딩이란 무엇인가?
* 오버로딩이랑 매개변수의 자료형에 따라 호출되는 함수가 달라지는 것을 뜻한다.

### 함수 오버로딩은 왜 필요한가?

```cpp
int AddInt(int a, int b)
double AddDouble(double a, double b)
```

이런 함수들을 만들어 일일히 구분하여 사용할 필요없이

```cpp
int Add(int a, int b)
double Add(double a, double b)
```

이런 식으로 선언하게 되면 Add함수에 들어가는 매개변수의 자료형에 따라 각각 타입에 맞는 함수가 호출되게 된다.

### namespace란 무엇인가?
* namespace란 말그대로 이름 영역으로, 여러 함수나 변수 등등이 존재할 수 있는 영역이다.

### namespace가 필요한 이유는?
만약 여러 프로그래머들이 협업 중이라고 하자.  
그러다가 동일한 이름을 가진 함수가 만들어졌다.  
일반적인 코딩에서는 충돌이 일어나겠지만,

각 프로그래머 별로 네임스페이스를 분리하여
```cpp
A::func();
B::func();
```
이런 식으로 사용하면 충돌을 방지 할수 있다.

흔히 사용되는

```cpp
using namespace std;
```

에서 std도 이러한 네임스페이스의 일종이라고 할수있다.

---

## +++막간+++

### C++에서 변수 하나가 선언되어 사용되는 과정을 메모리 관점에서 설명해보자.

int a가 선언.  
이 a는 메모리의 코드, 브스스, 데이터, 힙, 스택 중,  
전역번수라면 Data or BSS 영역에, 지역 변수라면 스택 영역에 만들어지게된다.  
a는 int 자료형이므로 4바이트의 영역에  
리틀엔디안 or 빅엔디안 방식으로 저장한다.  

만약 이 a에 1을 더하는 연산을 한다고 하자.  
cpu가 코드영역에서 기계어로 변환된 상태로 저장되어있는 a+1연산을 읽는다.  
이후 메모리버스에서 a의 주소에 접근, 값을 읽고 ALU로 연산을 수행한다.  
이후 스택영역의 a에 a+1된 값을 덮어쓴다.  

---

### 포인터, 참조, 배열, 함수가 서로 어떻게 연결되는지 설명해보자.

```cpp
#include <iostream>
using namespace std;

// 1. [함수 + 참조] : 참조를 이용해 원본 배열 요소를 수정하는 함수
void doubleElement(int& element) {
    element *= 2; // 참조를 통해 원본 값 직접 수정
}

int main() {
    // 2. [배열] : 메모리에 연속된 공간 확보
    int myArr[3] = {10, 20, 30};

    // 3. [배열 + 포인터] : 배열 이름은 첫 번째 요소의 주소
    int* ptr = myArr; // ptr은 myArr[0]의 주소를 가리킴

    // 4. [포인터 연산] : 포인터를 이동하며 함수 호출
    for (int i = 0; i < 3; i++) {
        // *(ptr + i)는 배열의 각 요소 주소에 접근하는 것
        doubleElement(*(ptr + i)); 
    }

    // 5. [함수 포인터] : 함수 자체의 주소를 저장
    void (*funcPtr)(int&) = doubleElement;

    cout << myArr[0]; // 출력 결과: 20 (원본이 수정됨)
    return 0;
}
```

gpt햄한테 긁어왔는데  
이게 메모리에 코드, 데이터, 스택, 힙 중 어디에 선언되고  
어디에 가르키고  
이걸 이해할 수 있으면 뭐 얼추 이해한거라고 본다  
내 개념에선  
일단 doubleElement함수가 코드영역에 저장되고  
배열이 스택영역에 할당된다음  
ptr이 스택영역에 또 할당되고 myArr첫번째 원소의 주소를 담고  
반복문에서 코드 영역에있는 함수를 호출해서   
배열의 각요소를 참조하여  
cpu가 계산을 해서 값을 2배로 만들고  

함수 포인터가 스택영역에서 선언되어 code영역에 있는 함수의 주소를 저장한다......  
뭐이런식..?
