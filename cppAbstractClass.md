### 8/23 — abstract class + 총복습

#### Abstract class

> **추상 클래스란 무엇인가?**
순수가상함수를 하나 이상 가지고 있는 클래스

> **추상 클래스는 왜 필요한가?**
자식 클래스에게 가상함수에 대한 작성을 강제하기 위해

> **순수 가상 함수란 무엇인가?**
virtual func() = 0;
처럼 아무 내용도 없는 가상함수.
순수 가상함수의 경우 자식 클래스가 무조건 직접 구현하여야한다.
자식 클래스가 함수를 구현하지 않으면 자식 클래스역시 추상 클래스가 되어 객체를 생성할 수 없다.

> **추상 클래스와 일반 클래스의 차이는?**
추상 클래스는 일반 클래스와 달리 객체를 만들수 없다.

> **추상 클래스의 객체를 직접 만들 수 없는 이유는?**
순수가상함수라는 구현되지 않은 인터페이스가 포함되어 있기때문에 객체를 생성할 수 없다.

> **추상 클래스와 다형성은 어떤 관계인가?**
추상 클래스를 이용하면 다형성을 이용할 때
자식 클래스에게 함수 작성을 강제할 수 있기 때문에
매우 유용하다.

> **게임 서버에서 추상 클래스를 사용할 만한 상황은?**
뭐 매 프레임 호출해야하는 PacketHandler 부모클래스로 AttackHandler, DefendHandler 이런걸 가르키고 있을때
패킷핸들러가 순수 가상함수로 Handler()를 만들면
자식 클래스가 Handler()를 구현하지 않았을 때, 객체를 만들려고하는 시점에 오류를 발생시켜
사전에 버그를 방지할 수 있다.

