# 배열

2026-08-14

## 배열이란 무엇인가?

배열은 변수 하나에 여러 값을 할당할 수 있는?
그런 느낌의 기능이다.

같은 자료형의 여러 값을 하나의 이름으로 관리 가능.

---

## 배열은 메모리에 어떻게 저장되는가?

연속적으로 자료형의 크기 × 배열의 길이로 메모리가 할당되고
그곳에 저장이 된다.

---

## 배열의 이름은 무엇을 의미하는가?

배열의 이름 `a`는 보통
첫 원소의 주솟값(첫 번째 원소를 가리키는 포인터)으로 변환이 된다.

단 `&a`, `sizeof(a)` 등 예외도 존재.

---

## `a`, `&a[0]`, `&a`는 어떻게 다른가?

앞 두 개는 유사한 의미이나,
`&a` 같은 경우 `+1` 연산 시 배열 전체의 크기만큼 늘어난다.

---

## 함수에 배열을 전달하면 실제로 무엇이 전달되는가?

첫 번째 주소값(첫 번째 원소를 가리키는 포인터)이 전달이 되겠지요.
