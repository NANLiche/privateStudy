# 참조에 대해 아무렇게나 써보기

20226-08-12

## 참조(reference)란 무엇인가?

참조란 c에는 없고 c++에만 있는 기능인데
var1 이라는 변수가 있다고 치면
var2로 var1을 참조하게 될 때
var2라는 이름을 가진 변수는 var1과 정확하게 동일한 기능을 수행하며
메모리 주소도 같고, 그러니까
한 주소를 두개의 이름이 공유하게 된다고 할 수 있다.

## 참조는 내부적으로 무엇을 의미하는가?

내부적으로는 다음과 같은 형태라고 할 수 있다

-----메모리-----
|var1=var2|
|(  값  )|
--------------

## 참조를 선언하고 사용하는 방법은?

참조를 선언하는 방법은 간단허다.

int var1 = 3;
int &var2 = var1;

이렇게 하면 var2가 var1을 참조하게 된다.
참고로 참조는 항상 선언하면서 초기화를 시켜주어야한다.
안그러면 오류나는 걸로 기억함.

## 포인터와 참조의 차이는?

포인터는 주솟값을 담는 변수인데 반해,
참조는 참조한 변수의 주소값을 함께 공유하는 느낌이다.
즉, 참조한 변수와 이름만 다르고 같은 공간을 쓰는 깐부라고 할수 있다.
따라서 포인터는 역참조 연산자를 통해 값을 얻어오는 반면
참조시에는 이름만 다르고 같은 변수이기 때문에 var2 = 3; 따위의 연산이 가능하다.

## 함수에서 참조를 사용하는 이유는?

보통함수에서 인수로 값을 받아올 때,

void func(int a, int b)

이런 식으로 작성이 되는데,
이는 완전히 새로운 주솟값에 새로운 변수(a)를 만들어 새로운 값을 할당하게 된다.
이와 같은 경우, func(var1, var2)
처럼 사용하면 값은 같을지라도 var1과 복사된 a는 완전히 다른 주소를 가지게 된다.
즉, a를 변경해도 var1의 값은 변하지 않는다.

이를 해결하기 위해서

int a 를 int &a처럼 사용하게되면
a는 var1의 이름을 공유하기 때문에
a의 값을 변경하게되면 var1의 값도 바뀌게된다.

포인터를 이용하려면
int *a처럼 쓴뒤
func(&var1, &var2)처럼 사용해서
*a를 변경하면 var1의 값도 변경되게 된다.
(a가 var1의 주솟값을 담고 있으므로)

## 참조와 const의 관계는?

const int &var2 = var1;

처럼 사용시
var2를 이용해서는 var1의 값을 변경하기 못하도록 만들수 있다.
