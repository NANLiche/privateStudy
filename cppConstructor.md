# 생성자
**20260824**
  
### 생성자란 무엇인가?
객체가 생성될 때 호출되는 함수

### 생성자는 언제 호출되는가?
객체가 생성될 때
  
### 생성자는 왜 필요한가?
객체를 생성함과 동시에 초기화 하기 위해  
=객체가 쓰레기 값을 가지게하지 않기 위해
  
### 생성자에는 어떤 종류가 있는가?
* 기본 생성자
* 매개변수가 있는 생성자
* 복사 생성자
* 이동 생성자
  * 매개변수 별로 오버로딩이 가능

### 생성자와 일반 멤버 함수의 차이는?
일반 멤버 함수는 여러번 호출할수있지만  
생성자는 객체가 생성될때 자체적으로 호출되는 함수기 때문에  
여러번 호출할 수 없다  
또한 생성자는 항상 클래스의 이름과 같고  
반환형이 없다.
  
### 객체가 생성될 때 메모리에서는 어떤 일이 일어나는가?
1. 객체 전체의 메모리가 할당
2. 멤버 변수들이 연속적인 메모리(중간에 padding이 있을 수 있다)에 초기화됨
3. 생성자 본문 실행
